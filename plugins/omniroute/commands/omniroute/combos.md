---
description: List all configured OmniRoute routing combos
---

Call `omniroute_list_combos` on the `omniroute` MCP server (include metrics when the tool
supports it) and present the combos as a table with columns: name, strategy, active/inactive,
and the providers each combo routes across.
