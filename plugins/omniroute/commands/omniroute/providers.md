---
description: Show OmniRoute provider connection health and latency metrics
---

Call `omniroute_get_provider_metrics` on the `omniroute` MCP server for all configured providers
and present a table with p50/p95/p99 latency and circuit breaker state per provider.
